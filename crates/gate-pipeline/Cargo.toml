[package]
name = "gate-pipeline"
version.workspace = true
edition.workspace = true
publish.workspace = true

[dependencies]
host = { version = "0.1.0", path = "../host" }

[dev-dependencies]
tempfile = "3.27.0"
